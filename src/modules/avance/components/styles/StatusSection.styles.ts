import { StyleSheet } from "react-native";

const statusSectionStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 8,
  },
  label: {
    fontSize: 14,
    color: "#333",
    marginRight: 8,
  },
});

export default statusSectionStyles;
